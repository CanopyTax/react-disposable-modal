{
  "name": "react-disposable-modal",
  "version": "0.2.3",
  "description": "React-disposable-modal is a reactive API for building modal components in React.",
  "main": "lib/disposable-modal.js",
  "repository": "https://github.com/CanopyTax/react-disposable-modal",
  "author": "Bret Little <bret.little@gmail.com>",
  "license": "MIT",
  "scripts": {
    "prepublish": "npm run build",
    "build": "babel src --out-dir lib --source-maps",
    "test": "jest --coverage --config ./jest.json",
    "test-watch": "jest --watch --config ./jest.json"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1"
  },
  "dependencies": {
    "react-disposable-decorator": "^2.1.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "rx": "^4.1.0",
    "disposable-component": "^0.2.0"
  },
  "files": [
    "lib",
    "src"
  ]
}
